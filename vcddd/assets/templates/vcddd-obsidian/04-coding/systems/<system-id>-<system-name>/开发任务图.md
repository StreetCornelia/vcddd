---
title: "{{系统名称}} - 开发任务图"
tags: ["vcddd/coding", "vcddd/development-task-graph"]
vcddd_type: "development-task-graph"
stage: "coding"
system_id: "{{SYS-NNN}}"
system_name: "{{系统名称}}"
created: "{{YYYY-MM-DD}}"
updated: "{{YYYY-MM-DD}}"
---

# {{系统名称}} - 开发任务图

> 仅当本轮存在两个及以上任务、且任务之间有实际依赖或共享写入冲突时创建本文。单一独立任务直接创建任务文档。

## 本轮目标

- 可交付能力：{{能力结果}}
- 范围边界：{{明确排除内容}}

## 任务与依赖

| 任务 | 可交付结果 | 依赖 | 依赖或冲突原因 |
|---|---|---|---|
| [[tasks/TASK-{{SYS-NNN}}-{{NNN}}-{{任务名称}}/任务]] | {{实际可用结果}} | {{任务 ID、外部结果或无}} | {{没有则省略}} |

## 依赖图（按需）

仅在依赖关系不易从上表理解时保留此图；没有依赖时删除本节。

```mermaid
flowchart LR
  T001["任务 A"] --> T002["任务 B"]
```

## 共享写入与外部依赖（按需）

| 路径、资源或外部结果 | 涉及任务 | 处理方式 |
|---|---|---|
| `{{path-or-resource}}` | {{任务 ID}} | {{边界、顺序或验证方式}} |

## 未决项（按需）

- {{尚未确定、会影响任务关系或范围的事项；没有则省略}}
